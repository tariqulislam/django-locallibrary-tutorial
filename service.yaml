apiVersion: v1
kind: Service
metadata:
  name: django-service
  labels:
    app: django-service
spec:
  type: LoadBalancer
  selector:
    app: django
  ports:
    - port: 8000
      targetPort: 8000
